{
  "name": "@ai-plugins/renderer",
  "version": "0.0.0",
  "private": true,
  "description": "Renders templates/ into per-agent plugins at the repo root. Dev-only; never published.",
  "type": "module",
  "bin": {
    "ai-plugins-renderer": "./src/cli.ts"
  },
  "scripts": {
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-plugins/schema": "workspace:*",
    "eta": "latest",
    "yaml": "latest"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
