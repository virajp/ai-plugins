{
  "name": "@ai-plugins/build",
  "version": "0.0.0",
  "private": true,
  "description": "Renders templates/ into per-agent plugins under dist/. Dev-only; never published.",
  "type": "module",
  "bin": {
    "ai-plugins-build": "./src/cli.ts"
  },
  "scripts": {
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-plugins/schema": "workspace:*",
    "eta": "latest",
    "yaml": "latest"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
