graphify-out/graph.json merge=graphify

# Rendered output. Committed so users install inspectable, CI-verified
# artifacts, but marked generated so it collapses in review — every byte under
# dist/ is a pure function of templates/.
dist/** linguist-generated=true
