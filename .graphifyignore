# Committed but machine-written or superseded — not code intelligence.
# .gitignore already covers bin/, dist/, docs/scratchpad/, graphify-out/.

# generated from the per-plugin manifests; the manifests themselves are indexed
.claude-plugin/

# the memory mirror (mempalace owns recall)
docs/memory/

# the Astro build output (gitignored too)
site/dist/
