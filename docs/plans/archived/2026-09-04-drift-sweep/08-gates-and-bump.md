# U8 — gates and bump

- **Wave:** 3
- **Depends on:** U6, U7
- **Owns:** `plugins/vwf/.claude-plugin/plugin.json`,
  `plugins/stackgen/.claude-plugin/plugin.json`,
  `.claude-plugin/marketplace.json`, `plugins/stackgen/stacks/inventory.md`
- **Model:** inherit
- **Read first:** both `plugin.json` files; index.md's Consent block.
- **Lazy-load:** `plugins/stackgen/assets/output-tree.md` (the four
  materialization targets, for the description fix), `.claude/docs/plugins.md`
  (the manifest shape).

## Ruling

Quoted from index.md's Consent block and decisions:

> Release `vwf` — minor. Release `stackgen` — minor. Release installer — none.

> 23 — Installer release: None — no `installer/` source changes.

> Shared-file rule: `plugins/*/.claude-plugin/plugin.json` — stackgen's
> description fix rides the bump.

Survey finding 12 for stackgen, deferred to this unit because of the shared-file
rule: the `plugin.json` description says "Both paths land directly in the repo's
committed `.claude/` tree", while `assets/output-tree.md` defines four targets,
two outside `.claude/` (a pack's `config/` tree mirroring the repo root, and the
generated local plugin at `~/.claude/plugins/local/stackgen-lsp/`).

## Edits

1. **`plugins/vwf/.claude-plugin/plugin.json`** — `version` `19.10.0` →
   `19.11.0`. Nothing else.
2. **`plugins/stackgen/.claude-plugin/plugin.json`** — `version` `0.21.0` →
   `0.22.0`; `description`: replace the "Both paths land directly in the repo's
   committed `.claude/` tree" clause with one that says a materialization lands
   in the repo's `.claude/` tree and, for packs that ship one, a `config/` tree
   mirroring the repo root, with language servers in a generated local plugin
   outside the repo. Keep the description one string, same length class as
   today.
3. **`.claude-plugin/marketplace.json`** and
   **`plugins/stackgen/stacks/inventory.md`** — regenerated by
   `mise run plugins:marketplace` and `mise run plugins:inventory`, never edited
   by hand. The marketplace refs must read `vwf-v19.11.0` and
   `stackgen-v0.22.0`.

## Verification

The full wave gate from index.md, all green:

- `mise run plugins:check` (twelve rules)
- `mise run plugins:marketplace --check`
- `mise run plugins:inventory --check`
- `pnpm vitest run`
- `pnpm exec tsc --noEmit -p installer` and `-p scripts`
- `mise run plugins:npm-normalize-test`
- `mise run code:format`, `mise run code:lint`
- `claude plugin validate --strict` on both plugins (the check task runs it)
- The plan-specific greps in index.md's wave gate return only historical rows.
- **`target-verifier`**, dispatched by the orchestrator with this plan's summary
  of what changed. Pass condition: both plugins install hermetically
  (`CLAUDE_CONFIG_DIR=/tmp/…`) from `./.dev-marketplace` at `19.11.0` and
  `0.22.0`; `claude plugin validate --strict` passes; in a scratch repo with a
  Flutter project pinned, `/vwf:doctor`'s harness check reads a stamp carrying
  `goldens` without reporting an unknown key.
- `mise run plugins:local` on the authoring machine is **not** run by the unit;
  the orchestrator notes it for the user after landing.

## Guardrails

- Never run `plugins:release` or `i:release`; execute-plan stops before the
  `main` merge and asks.
- Do not edit any file outside the four owned; a gate failure elsewhere is
  returned as `UNRESOLVED:` naming the unit whose tree it is in.
- A tracked plugin version is plain `X.Y.Z`; no `+N`.
- `plugins:check` refuses a description that violates its prose rules — read the
  failure, not the rule list, if it fails.

## Commit

`ops(release): vwf 19.11.0 and stackgen 0.22.0 after the drift sweep` — written
by the orchestrator after the wave gate, not by the unit.
