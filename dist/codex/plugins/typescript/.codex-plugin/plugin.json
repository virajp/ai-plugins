{
  "name": "typescript",
  "version": "1.0.0",
  "description": "Opinionated plain-TypeScript plugin — a `typescript` router skill (lean SKILL.md routing to on-demand references: the coding standards baseline, Vitest testing, the build pipeline for single-package and monorepo) plus `package-json`, `pnpm`, `tsconfig`, and `lint-format`, and the TypeScript/JavaScript language server. Effect-TS doctrine lives in the separate `effect` plugin, which depends on this one.",
  "author": {
    "name": "Viraj Patel"
  },
  "keywords": [
    "typescript",
    "javascript",
    "pnpm",
    "tsconfig",
    "monorepo",
    "build",
    "vitest",
    "testing",
    "skills",
    "language-server",
    "lsp"
  ]
}
