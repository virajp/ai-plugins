## Setup

Replace `MaterialApp` with `GetMaterialApp`. No other global configuration is
required.

```dart
import 'package:get/get.dart';

void main() => runApp(
  GetMaterialApp(
    home: HomeScreen(),
  ),
);
```

---
