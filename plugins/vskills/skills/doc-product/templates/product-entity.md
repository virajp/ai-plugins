---
status: untriaged
---

# <Entity>

One-sentence description of what this entity is and why it exists for the rider.

## Overview

Key concepts, types, and states visible to the user (e.g. ride types: private,
public; group membership states: pending, active, banned).

## User Roles

Who interacts with this entity and in what capacity. List every role that has
different permissions or visibility (e.g. Owner, Member, Guest, Admin).

## General Rules

Rules that apply regardless of action — constraints, limits, and invariants the
user can observe.

## Out of Scope

What this entity explicitly does NOT cover. This section is required and must
not be left blank.

## Abuse & Trust Vectors

Known ways this entity could be misused. Describe each from the user's
perspective (no implementation mitigations here — just the risk).

## Data Model

> See [<Entity> Schema](../../engineering/common/schemas/<entity>.md) for the
> Firestore document structure.

## Actions

- [Action Name](./<action>.md) — one-line description of what the user
  accomplishes

