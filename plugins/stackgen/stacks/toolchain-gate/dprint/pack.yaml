name: dprint
summary: The repo's single formatter — one root config, plugins pinned,
  generated trees excluded.
version: 0.1.0
type: toolchain-gate
kind: repo-gate
axis: repo
# Topic 1 (format authority) of the repo-gate bar (assets/kinds.md).
# Gates satisfy no vwf harness capability.
harness: n/a
