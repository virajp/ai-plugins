#!/usr/bin/env bash
#MISE description="Move the lockfile forward, then re-install"
#MISE hide=true

set -e
source "${MISE_PROJECT_ROOT}/.config/mise/tasks/_scripts/helpers"

print_header "Upgrading uv lockfile ..."
uv lock --upgrade
uv sync --all-extras
