#!/usr/bin/env bash
#MISE description="Install dependencies from the lockfile"
#MISE hide=true

set -e
source "${MISE_PROJECT_ROOT}/.config/mise/tasks/_scripts/helpers"

print_header "Syncing uv environment ..."
uv sync --all-extras
