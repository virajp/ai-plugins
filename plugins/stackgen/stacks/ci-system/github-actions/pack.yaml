name: GitHub Actions
summary: The delivery pipeline on GitHub Actions — workflow layout, toolchain
  installation through mise, the gate sequence, and vwf's tag-triggered release
  contract.
version: 0.1.0
type: ci-system
capability: n/a
kind: ci-system
axis: cicd
# The pipeline satisfies no harness capability itself — it invokes the repo's
# task names, which it must not invent.
harness: n/a
