name: OIDC issuer
summary: Identity as an open protocol rather than a product — any issuer
  speaking OpenID Connect, self-hosted or managed.
version: 0.1.0
type: capability-provider
category: identity
capability: third-party-auth
kind: capability-provider
axis: backing
harness:
  local_stack:
    task: stack:up
    mechanism: >-
      Docker-composed OIDC issuer behind a wait-on readiness gate, seeded with the acceptance suite's test principals
