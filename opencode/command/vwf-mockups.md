---
description: "Render the blueprint's screens as self-contained static HTML mockups — one page per screen plus the state variants the Screens contract pins, styled from design-system tokens — into the repo's gitignored docs/scratchpad/ tree for local browser review. Mockups are realizations, never contract; never pushed to the design tool, never committed."
---

Read the `mockups` workflow skill at `%%AI_PLUGINS_ROOT:vwf%%/commands/mockups/index.md` and follow
it for this request.

Arguments ([flow, e.g. checkout — omit to sweep all screens]):

$ARGUMENTS
