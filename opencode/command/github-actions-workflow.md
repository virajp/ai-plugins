---
description: "Generate a GitHub Actions workflow that installs every tool through the jdx/mise-action (mise only) and supports both polyrepo and monorepo layouts. Detects the repo, asks what to generate, then writes .github/workflows/<name>.yml."
---

Read the `workflow` workflow skill at `%%AI_PLUGINS_ROOT:github-actions%%/commands/workflow/index.md` and follow
it for this request.

Arguments ([workflow-name | ci | release | deploy]):

$ARGUMENTS
