{
  "name": "@ai-plugins/scripts",
  "version": "0.0.0",
  "private": true,
  "description": "Repo tooling for the authored plugins/ tree: generates .claude-plugin/marketplace.json and statically validates every plugin. Run directly by node — nothing here is published.",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "latest"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
